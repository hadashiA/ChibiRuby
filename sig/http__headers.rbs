# <auto-generated />
# Source: MRubyCS.StdLib.HttpHeadersMembers

class HTTP::Headers < Object
  # headers[name] — case-insensitive lookup. Returns the first matching value, or nil if absent.
  def []: (String) -> String?

  # headers[name] = value — replaces every same-named entry (case-insensitive); appends if absent.
  def []=: (String, String) -> String

  # headers.key?(name) — true iff the name is set (case-insensitive).
  def key?: (String) -> bool

  # headers.each { |name, value| … } — yields each entry in insertion order. Returns an Array of [name, value] pairs (the yielded values) so it works as an iterator chain even without a block.
  def each: () { (String, String) -> void } -> Array[Array[String]]

  # headers.to_h — flat Hash[String, String] snapshot. Duplicate header names collapse to the last value (Ruby Hash semantics).
  def to_h: () -> Hash[String, String]

  # headers.size — number of header entries (counting duplicates separately).
  def size: () -> Integer
  alias length size

  # headers.inspect — Ruby-debug representation as a Hash literal of the underlying entries (duplicates collapse).
  def inspect: () -> String

end
