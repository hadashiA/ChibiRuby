# <auto-generated />
# Source: MRubyCS.StdLib.MatchDataMembers

# Result of a successful regexp match -- returned by Regexp#match and available as the $~ implicit variable after a match. Indexing (md[0], md[1], ...) returns the matched substring or capture groups; named captures are accessed by symbol or string.
class MatchData < Object
  # Returns a capture group by integer index, name, or range. With two integer arguments returns a sub-array of captures.
  #
  #   m = /(\w+) (\w+)/.match("hello world")
  #   m[0]      # => "hello world"
  #   m[1]      # => "hello"
  #   m[1, 2]   # => ["hello", "world"]
  def []: (Integer | Symbol | String | Range[Integer]) -> String? | (Integer, Integer) -> Array[String?]?

  # Returns the character offset of the start of the nth capture group, or nil when the group did not participate in the match.
  #
  #   m = /(\w+)/.match("hello world")
  #   m.begin(0)   # => 0
  #   m.begin(1)   # => 0
  def begin: (Integer) -> Integer?

  # Returns the character offset just past the end of the nth capture group, or nil when the group did not participate in the match.
  #
  #   m = /(\w+)/.match("hello world")
  #   m.end(1)   # => 5
  def end: (Integer) -> Integer?

  # Returns a two-element array [begin, end] giving the character offsets of the nth capture group.
  #
  #   m = /(\w+) (\w+)/.match("hello world")
  #   m.offset(2)   # => [6, 11]
  def offset: (Integer) -> Array[Integer?]

  # Returns the captured groups from self as an array, excluding the whole match at index 0.
  #
  #   m = /(\w+) (\w+)/.match("hello world")
  #   m.captures   # => ["hello", "world"]
  def captures: () -> Array[String?]

  # Returns the whole match followed by every capture as an array.
  #
  #   m = /(\w+) (\w+)/.match("hello world")
  #   m.to_a   # => ["hello world", "hello", "world"]
  def to_a: () -> Array[String?]

  # Returns the entire matched substring of self.
  #
  #   m = /(\w+) (\w+)/.match("hello world")
  #   m.to_s   # => "hello world"
  def to_s: () -> String

  # Returns the number of elements in self, including the whole match plus all capture groups.
  #
  #   m = /(\w+) (\w+)/.match("hello world")
  #   m.size   # => 3
  def size: () -> Integer

  # Alias for size. Returns the number of elements in self (the whole match plus captures).
  #
  #   m = /(\w+) (\w+)/.match("hello world")
  #   m.length   # => 3
  def length: () -> Integer

  # Returns the portion of the original string before the match.
  #
  #   m = /world/.match("hello world!")
  #   m.pre_match   # => "hello "
  def pre_match: () -> String

  # Returns the portion of the original string after the match.
  #
  #   m = /world/.match("hello world!")
  #   m.post_match   # => "!"
  def post_match: () -> String

  # Returns the Regexp that produced self.
  #
  #   m = /(\w+)/.match("hello")
  #   m.regexp   # => /(\w+)/
  def regexp: () -> Regexp

  # Returns the frozen copy of the original string that was matched against.
  #
  #   m = /world/.match("hello world")
  #   m.string   # => "hello world"
  def string: () -> String

  # Returns a hash mapping each named capture group to its matched substring, or nil when the group did not participate.
  #
  #   m = /(?<y>\d{4})-(?<m>\d{2})/.match("2024-01")
  #   m.named_captures   # => {"y" => "2024", "m" => "01"}
  def named_captures: () -> Hash[String, String?]

  # Returns the list of named capture group names defined by the regexp that produced self.
  #
  #   m = /(?<y>\d{4})-(?<m>\d{2})/.match("2024-01")
  #   m.names   # => ["y", "m"]
  def names: () -> Array[String]

  # Returns an array of the elements at the given integer indices into self.
  #
  #   m = /(\w+) (\w+) (\w+)/.match("a b c")
  #   m.values_at(1, 3)   # => ["a", "c"]
  def values_at: (*Integer) -> Array[String?]

  # Returns a human-readable representation of self showing the whole match and each capture group.
  #
  #   m = /(\w+) (\w+)/.match("hello world")
  #   m.inspect   # => "#<MatchData \"hello world\" 1:\"hello\" 2:\"world\">"
  def inspect: () -> String

end
