# <auto-generated />
# Source: MRubyCS.StdLib.RandomMembers

# Seeded pseudo-random number generator. Random.new(seed) creates an independent stream; Kernel#rand and Kernel#srand delegate to a shared default instance. Backed by .NET's Random in MRubyCS, so the sequence is not bit-compatible with CRuby.
class Random < Object
  # Initializes a new Random instance, optionally with a fixed seed.
  #
  #   r = Random.new(42)
  #   r.rand(100)         # => Integer in 0..99
  def initialize: (?Integer) -> void

  # Returns a pseudo-random number using this Random instance.
  #
  #   r = Random.new(1)
  #   r.rand(10)          # => Integer in 0..9
  def rand: (?(Integer | Float | Range[Numeric])) -> (Integer | Float)

  # Seeds this Random instance and returns the previous seed.
  #
  #   r = Random.new
  #   r.srand(42)         # => previous seed
  def srand: (?Integer) -> Integer

  # Returns a String of n random bytes from this Random instance.
  #
  #   r = Random.new(1)
  #   r.bytes(4)          # => 4-byte random String
  def bytes: (Integer) -> String

  # Returns a pseudo-random number using the default Random instance. With no argument returns a Float in [0.0, 1.0); with an Integer n returns an Integer in [0, n); with a Range returns a value in that range.
  #
  #   Random.rand         # => Float
  #   Random.rand(10)     # => Integer in 0..9
  def self.rand: (?(Integer | Float | Range[Numeric])) -> (Integer | Float)

  # Seeds the default Random instance and returns the previous seed. With no argument re-seeds from the system clock.
  #
  #   Random.srand(42)    # => previous seed
  def self.srand: (?Integer) -> Integer

  # Returns a String of n random bytes using the default Random instance.
  #
  #   Random.bytes(4)     # => 4-byte random String
  def self.bytes: (Integer) -> String

end
