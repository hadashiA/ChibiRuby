# <auto-generated />
# Source: ChibiRuby.StdLib.IntegerMembers

# Arbitrary-precision-style integer (backed by a 64-bit value in ChibiRuby). Created by integer literals like 42 or 0xff, and used for counting, indexing, and bit manipulation. Integer is immutable, includes Comparable, and supports the full set of numeric and bitwise operators.
class Integer < Numeric
  # Returns self raised to the power of the argument.
  #
  #   2 ** 10    # => 1024
  #   3 ** 0     # => 1
  #   2 ** -1    # => 0.5
  def **: (Numeric) -> Numeric

  # Returns the sum of self and the argument.
  #
  #   1 + 2      # => 3
  #   1 + 2.5    # => 3.5
  def +: (Numeric) -> Numeric

  # Returns the difference of self minus the argument.
  #
  #   5 - 2      # => 3
  #   1 - 0.5    # => 0.5
  def -: (Numeric) -> Numeric

  # Returns the product of self and the argument.
  #
  #   3 * 4      # => 12
  #   2 * 1.5    # => 3.0
  def *: (Numeric) -> Numeric

  # Divides self by the argument. Integer division truncates toward zero. Raises ZeroDivisionError when the argument is zero.
  #
  #   10 / 3     # => 3
  #   10 / 3.0   # => 3.3333333333333335
  def /: (Numeric) -> Numeric

  # Returns the string representation of self in the given base (default 10).
  #
  #   12345.to_s        # => "12345"
  #   255.to_s(16)      # => "ff"
  #   10.to_s(2)        # => "1010"
  def to_s: (?Integer) -> String
  alias inspect to_s

  # Returns self modulo the argument. The sign of the result follows the divisor.
  #
  #   10 % 3      # => 1
  #   (-10) % 3   # => 2
  #   10 % -3     # => -2
  def %: (Numeric) -> Numeric

  # Unary plus; returns self unchanged.
  #
  #   +5     # => 5
  #   +(-3)  # => -3
  def +@: () -> Integer

  # Unary minus; returns self negated.
  #
  #   -5     # => -5
  #   -(-3)  # => 3
  def -@: () -> Integer

  # Returns the integer quotient of self divided by the argument.
  #
  #   11.div(3)    # => 3
  #   (-11).div(3) # => -4
  def div: (Numeric) -> Integer

  # Returns the floating-point quotient of self divided by the argument.
  #
  #   10.fdiv(3)    # => 3.3333333333333335
  #   4.fdiv(2)     # => 2.0
  def fdiv: (Numeric) -> Float

  # Returns the absolute value of self.
  #
  #   5.abs       # => 5
  #   (-7).abs    # => 7
  def abs: () -> Integer

  # Returns the quotient of self divided by the argument as a Float.
  #
  #   10.quo(3)    # => 3.3333333333333335
  #   7.quo(2)     # => 3.5
  def quo: (Numeric) -> Numeric

  # Returns self rounded up to the nearest multiple of 10**-ndigits. With no argument or non-negative ndigits, returns self.
  #
  #   1234.ceil(-2)    # => 1300
  #   1234.ceil        # => 1234
  def ceil: (?Integer) -> Integer

  # Returns self rounded down to the nearest multiple of 10**-ndigits. With no argument or non-negative ndigits, returns self.
  #
  #   1234.floor(-2)   # => 1200
  #   1234.floor       # => 1234
  def floor: (?Integer) -> Integer

  # Returns self rounded to the nearest multiple of 10**-ndigits. With no argument or non-negative ndigits, returns self.
  #
  #   1234.round(-2)   # => 1200
  #   1250.round(-2)   # => 1300
  def round: (?Integer) -> Integer

  # Returns the integer that is one greater than self.
  #
  #   1.next     # => 2
  #   (-3).next  # => -2
  def next: () -> Integer
  alias succ next

  # Returns self truncated toward zero to a multiple of 10**-ndigits. With no argument or non-negative ndigits, returns self.
  #
  #   1234.truncate(-2)    # => 1200
  #   1234.truncate        # => 1234
  def truncate: (?Integer) -> Integer

  # Returns a hash code for self, suitable for use as a Hash key.
  #
  #   1.hash == 1.hash    # => true
  def hash: () -> Integer

  # Returns a two-element array containing the integer quotient and modulus.
  #
  #   11.divmod(3)     # => [3, 2]
  #   (-11).divmod(3)  # => [-4, 1]
  def divmod: (Numeric) -> Array[Numeric]

  # Returns the value of self as a Float.
  #
  #   1.to_f       # => 1.0
  #   (-5).to_f    # => -5.0
  def to_f: () -> Float

  # Returns the number of bits in the two's-complement representation of self, excluding the sign bit.
  #
  #   0.bit_length     # => 0
  #   255.bit_length   # => 8
  #   (-256).bit_length # => 8
  def bit_length: () -> Integer

  # Returns the greatest common divisor of self and the argument (always non-negative).
  #
  #   12.gcd(8)      # => 4
  #   18.gcd(24)     # => 6
  def gcd: (Integer) -> Integer

  # Returns the least common multiple of self and the argument (always non-negative). Returns 0 if either operand is 0.
  #
  #   4.lcm(6)      # => 12
  #   18.lcm(24)    # => 72
  def lcm: (Integer) -> Integer

  # Returns the bitwise AND of self and the argument.
  #
  #   0b1100 & 0b1010    # => 8
  #   5 & 3              # => 1
  def &: (Integer) -> Integer

  # Returns the bitwise OR of self and the argument.
  #
  #   0b1100 | 0b1010    # => 14
  #   5 | 3              # => 7
  def |: (Integer) -> Integer

  # Returns the bitwise exclusive OR of self and the argument.
  #
  #   0b1100 ^ 0b1010    # => 6
  #   5 ^ 3              # => 6
  def ^: (Integer) -> Integer

  # Returns self shifted left by the given number of bits. Negative shifts are equivalent to right shifts.
  #
  #   1 << 4    # => 16
  #   5 << 2    # => 20
  def <<: (Integer) -> Integer

  # Returns self shifted right (arithmetically) by the given number of bits. Negative shifts are equivalent to left shifts.
  #
  #   16 >> 2    # => 4
  #   20 >> 2    # => 5
  def >>: (Integer) -> Integer

  # --- from lib.rb ---
  #
  # Calls the given block once for each Integer
  # from +self+ downto +num+.
  #
  # ISO 15.2.8.3.15
  def downto: (untyped num) ?{ (?) -> untyped } -> (untyped | self)

  #
  # Calls the given block +self+ times.
  #
  # ISO 15.2.8.3.22
  def times: () ?{ (?) -> untyped } -> (untyped | self)

  #
  # Calls the given block once for each Integer
  # from +self+ upto +num+.
  #
  # ISO 15.2.8.3.27
  def upto: (untyped num) ?{ (?) -> untyped } -> (untyped | self)
end
