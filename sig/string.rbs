# <auto-generated />
# Source: MRubyCS.StdLib.StringMembers

# Mutable sequence of bytes, usually interpreted as UTF-8 text. Indexing returns substrings or single characters; many methods come in pairs of pure (upcase) and in-place (upcase!) variants. Includes Comparable. String literals are frozen by some host configurations but mutable by default.
class String < Object
  # Initializes a new String, optionally copying the contents of the given string.
  #
  #   String.new            # => ""
  #   String.new("hello")   # => "hello"
  def initialize: (?String) -> void

  # Replaces the contents of self with a copy of the given string. Called by dup and clone.
  #
  #   "abc".dup   # => "abc"
  def initialize_copy: (String) -> self

  # Equality ==. Returns true when the argument is a String with the same byte content.
  #
  #   "abc" == "abc"   # => true
  #   "abc" == "abd"   # => false
  #   "abc" == :abc    # => false
  def eql?: (untyped) -> bool
  alias == eql?

  # Comparison <=>. Returns -1, 0, or 1 by comparing byte content with the given string, or nil for non-strings.
  #
  #   "abc" <=> "abd"    # => -1
  #   "abc" <=> "abc"    # => 0
  #   "abc" <=> 42       # => nil
  def <=>: (untyped) -> Integer?

  # Element reference []. Returns the substring at the given index, range, or (start, length), or nil when out of range.
  #
  #   "hello"[0]      # => "h"
  #   "hello"[1, 3]   # => "ell"
  #   "hello"[1..3]   # => "ell"
  def slice: (Integer | Range[Integer]) -> String? | (Integer, Integer) -> String?
  alias [] slice

  # Element assignment []=. Replaces the substring at the given index, range, or (start, length) with the given string.
  #
  #   s = "hello"
  #   s[0] = "H"        # s == "Hello"
  #   s[1, 3] = "i!"    # s == "Hi!o"
  def []=: (Integer, String) -> String | (Integer, Integer, String) -> String | (Range[Integer], String) -> String

  # Repetition *. Returns a new string built by concatenating self the given number of times.
  #
  #   "ab" * 3      # => "ababab"
  #   "x" * 0       # => ""
  def *: (Integer) -> String

  # Returns a quoted, escaped representation of self suitable for debugging output.
  #
  #   "hi\n".inspect    # => "\"hi\\n\""
  def inspect: () -> String

  # Returns the Symbol corresponding to self.
  #
  #   "foo".to_sym    # => :foo
  def to_sym: () -> Symbol

  # Returns self (or a duplicate of self when called on a plain String).
  #
  #   "hello".to_s     # => "hello"
  def to_s: () -> String

  # Parses the leading integer in self using the given radix (2, 8, 10, or 16; default 10). Returns 0 when no number can be parsed.
  #
  #   "42abc".to_i      # => 42
  #   "ff".to_i(16)     # => 255
  #   "1010".to_i(2)    # => 10
  #   "abc".to_i        # => 0
  def to_i: (?Integer) -> Integer

  # Parses the leading floating-point number in self and returns it. Returns 0.0 when no number can be parsed.
  #
  #   "3.14".to_f      # => 3.14
  #   "1e2".to_f       # => 100.0
  #   "abc".to_f       # => 0.0
  def to_f: () -> Float

  # Returns the number of characters (UTF-8 code points) in self.
  #
  #   "hello".size     # => 5
  #   "".size          # => 0
  def size: () -> Integer
  alias length size

  # Returns true when self has zero length, false otherwise.
  #
  #   "".empty?       # => true
  #   "x".empty?      # => false
  def empty?: () -> bool

  # Returns true when self contains the given substring.
  #
  #   "hello".include?("ell")   # => true
  #   "hello".include?("xyz")   # => false
  def include?: (String) -> bool

  # Returns the index of the first occurrence of the given substring (optionally starting at the given offset), or nil when not found.
  #
  #   "hello".index("l")        # => 2
  #   "hello".index("l", 3)     # => 3
  #   "hello".index("x")        # => nil
  def index: (String, ?Integer) -> Integer?

  # Returns the index of the last occurrence of the given substring (optionally ending at the given offset), or nil when not found.
  #
  #   "hello".rindex("l")       # => 3
  #   "hello".rindex("x")       # => nil
  def rindex: (String, ?Integer) -> Integer?

  # Returns the Symbol corresponding to self.
  #
  #   "foo".intern    # => :foo
  def intern: () -> Symbol

  # Replaces the contents of self with the contents of the given string and returns self.
  #
  #   s = "hello"
  #   s.replace("world")   # => "world"
  def replace: (String) -> self

  # Returns a copy of self with the trailing record separator (default "\n" or "\r\n") removed.
  #
  #   "hello\n".chomp        # => "hello"
  #   "hello\r\n".chomp      # => "hello"
  #   "hello".chomp("lo")    # => "hel"
  def chomp: (?String) -> String

  # Removes the trailing record separator from self in place. Returns nil.
  #
  #   s = "hello\n"
  #   s.chomp!     # => nil
  #   s            # => "hello"
  def chomp!: (?String) -> self?

  # Returns a copy of self with the last character removed ("\r\n" counts as one character).
  #
  #   "hello".chop       # => "hell"
  #   "hi\r\n".chop      # => "hi"
  def chop: () -> String

  # Removes the last character of self in place. Returns nil.
  #
  #   s = "hello"
  #   s.chop!     # => nil
  #   s           # => "hell"
  def chop!: () -> self?

  # Returns a copy of self with all lowercase ASCII letters converted to uppercase.
  #
  #   "Hello".upcase     # => "HELLO"
  def upcase: () -> String

  # Upcases self in place. Returns nil.
  #
  #   s = "Hello"
  #   s.upcase!     # => nil
  #   s             # => "HELLO"
  def upcase!: () -> self?

  # Returns a copy of self with all uppercase ASCII letters converted to lowercase.
  #
  #   "Hello".downcase     # => "hello"
  #   "ABC".downcase       # => "abc"
  def downcase: () -> String

  # Downcases self in place. Returns nil.
  #
  #   s = "Hello"
  #   s.downcase!    # => nil
  #   s              # => "hello"
  def downcase!: () -> self?

  # Returns a copy of self with the first character converted to uppercase and the rest to lowercase.
  #
  #   "hello".capitalize    # => "Hello"
  #   "HELLO".capitalize    # => "Hello"
  def capitalize: () -> String

  # Capitalizes self in place. Returns nil.
  #
  #   s = "hello"
  #   s.capitalize!   # => nil
  #   s               # => "Hello"
  def capitalize!: () -> self?

  # Returns a new string with the characters of self reversed.
  #
  #   "hello".reverse    # => "olleh"
  def reverse: () -> String

  # Reverses the characters of self in place and returns self.
  #
  #   s = "hello"
  #   s.reverse!    # => "olleh"
  def reverse!: () -> self

  # Splits self into an array of substrings using the given delimiter (string or Regexp). With no delimiter splits on whitespace.
  #
  #   "a,b,c".split(",")        # => ["a", "b", "c"]
  #   "a b  c".split            # => ["a", "b", "c"]
  #   "a,b,c".split(",", 2)     # => ["a", "b,c"]
  def split: (?(String | Regexp), ?Integer) -> Array[String]

  # Returns the number of bytes in self.
  #
  #   "hello".bytesize     # => 5
  #   "日本".bytesize      # => 6
  def bytesize: () -> Integer

  # Returns an array of the bytes that make up self.
  #
  #   "abc".bytes      # => [97, 98, 99]
  def bytes: () -> Array[Integer]

  # Returns the byte at the given byte index, or nil when out of range. Negative indices count from the end.
  #
  #   "abc".getbyte(0)    # => 97
  #   "abc".getbyte(-1)   # => 99
  #   "abc".getbyte(10)   # => nil
  def getbyte: (Integer) -> Integer?

  # Sets the byte at the given byte index to the low 8 bits of the given integer. Returns self.
  #
  #   s = "abc"
  #   s.setbyte(0, 65)    # s == "Abc"
  def setbyte: (Integer, Integer) -> Integer

  # Returns the byte offset of the first occurrence of the given substring (optionally starting at the given byte offset), or nil when not found.
  #
  #   "hello".byteindex("l")        # => 2
  #   "hello".byteindex("l", 3)     # => 3
  def byteindex: (String, ?Integer) -> Integer?

  # Returns the substring at the given byte index, byte range, or (start, length) in bytes. Returns nil when out of range.
  #
  #   "hello".byteslice(1)         # => "e"
  #   "hello".byteslice(1, 3)      # => "ell"
  #   "hello".byteslice(1..3)      # => "ell"
  def byteslice: (Integer, ?Integer) -> String?

  # Replaces the bytes at the given byte index, byte range, or (start, length) in self with the given string. Returns self.
  #
  #   s = "hello"
  #   s.bytesplice(1, 3, "i")    # s == "hio"
  def bytesplice: (*untyped) -> String

  # Internal helper used by String#sub to expand backreferences in a replacement pattern.
  def __sub_replace: (String, MatchData) -> String

  # --- from lib.rb ---
  # ISO 15.2.10.3
  include Comparable

  #
  # Calls the given block for each line
  # and pass the respective line.
  #
  # ISO 15.2.10.5.15
  def each_line: (?::String separator) ?{ (?) -> untyped } -> (untyped | self)

  #
  # Replace all matches of +pattern+ with +replacement+.
  # Call block (if given) for each match and replace
  # +pattern+ with the value of the block. Return the
  # final value.
  #
  # ISO 15.2.10.5.18
  def gsub: (*untyped args) ?{ (?) -> untyped } -> untyped

  #
  # Replace all matches of +pattern+ with +replacement+.
  # Call block (if given) for each match and replace
  # +pattern+ with the value of the block. Modify
  # +self+ with the final value.
  #
  # ISO 15.2.10.5.19
  def gsub!: (*untyped args) { (?) -> untyped } -> (untyped | nil)

  #
  # Replace only the first match of +pattern+ with
  # +replacement+. Call block (if given) for each
  # match and replace +pattern+ with the value of the
  # block. Return the final value.
  #
  # ISO 15.2.10.5.36
  def sub: (*untyped args) ?{ (?) -> untyped } -> (self | untyped)

  #
  # Replace only the first match of +pattern+ with
  # +replacement+. Call block (if given) for each
  # match and replace +pattern+ with the value of the
  # block. Modify +self+ with the final value.
  #
  # ISO 15.2.10.5.37
  def sub!: (*untyped args) { (?) -> untyped } -> (nil | untyped)

  #
  # Call the given block for each byte of +self+.
  def each_byte: () ?{ (?) -> untyped } -> (untyped | self)
end
