# <auto-generated />
# Source: MRubyCS.StdLib.HttpBodyMembers

class HTTP::Body < Object
  # body.to_s — the response body as a String (raw bytes; the caller decides how to decode it).
  def to_s: () -> String

  # body.bytesize — the byte length of the buffered body.
  def bytesize: () -> Integer

  # body.content_type — the parsed Content-Type header value, or nil if absent.
  def content_type: () -> String?

  # body.empty? — true iff the body has zero bytes.
  def empty?: () -> bool

  # body.each { |chunk| … } — yields the body to the block. v1 always yields a single chunk; the iteration shape is preserved so a future streaming implementation can yield piecewise without breaking existing Ruby code.
  def each: () { (String) -> void } -> self

  # body.inspect — preview of the body length, no contents.
  def inspect: () -> String

end
