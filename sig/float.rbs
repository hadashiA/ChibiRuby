# <auto-generated />
# Source: MRubyCS.StdLib.FloatMembers

# IEEE 754 double-precision floating-point number. Created by literals with a decimal point or exponent (e.g. 1.5, 2e10), or by mixed arithmetic with Integer. Values are immutable; Float implements Comparable and supports the usual arithmetic operators.
class Float < Numeric
  # Returns self raised to the power of the argument.
  #
  #   2.0 ** 3      # => 8.0
  #   9.0 ** 0.5    # => 3.0
  def **: (Numeric) -> Float

  # Returns the sum of self and the argument as a Float.
  #
  #   1.5 + 2.5     # => 4.0
  #   1.5 + 1       # => 2.5
  def +: (Numeric) -> Float

  # Returns the difference of self minus the argument as a Float.
  #
  #   2.5 - 1.0     # => 1.5
  #   5.0 - 2       # => 3.0
  def -: (Numeric) -> Float

  # Returns the product of self and the argument as a Float.
  #
  #   2.0 * 3.0     # => 6.0
  #   1.5 * 2       # => 3.0
  def *: (Numeric) -> Float

  # Returns the quotient of self divided by the argument as a Float.
  #
  #   10.0 / 4      # => 2.5
  #   1.0 / 0       # => Infinity
  def /: (Numeric) -> Float

  # Returns self modulo the argument as a Float.
  #
  #   6.5 % 2     # => 0.5
  #   6.0 % 2.5   # => 1.0
  def %: (Numeric) -> Float

  # Returns -1, 0, or 1 when self is less than, equal to, or greater than the argument. Returns nil if the argument is not comparable or either side is NaN.
  #
  #   1.5 <=> 2.0    # => -1
  #   1.5 <=> 1.5    # => 0
  #   1.5 <=> "x"    # => nil
  def <=>: (untyped) -> Integer?

  # Returns true when self equals the argument numerically.
  #
  #   1.0 == 1       # => true
  #   1.0 == 1.0     # => true
  #   1.0 == "1"     # => false
  def ==: (untyped) -> bool

  # Unary minus; returns self negated.
  #
  #   -1.5    # => -1.5
  #   -(-2.0) # => 2.0
  def -@: () -> Float

  # Returns the bitwise AND of self (converted to Integer) and the argument.
  #
  #   12.0 & 10    # => 8
  def &: (Integer) -> Integer

  # Returns the bitwise OR of self (converted to Integer) and the argument.
  #
  #   12.0 | 10    # => 14
  def |: (Integer) -> Integer

  # Returns the bitwise exclusive OR of self (converted to Integer) and the argument.
  #
  #   12.0 ^ 10    # => 6
  def ^: (Integer) -> Integer

  # Returns self shifted left by the given number of bits, converted as needed.
  #
  #   1.0 << 3    # => 8
  def <<: (Integer) -> Integer

  # Returns self shifted right by the given number of bits, converted as needed.
  #
  #   16.0 >> 2    # => 4
  def >>: (Integer) -> Integer

  # Returns the smallest number greater than or equal to self, with optional precision.
  #
  #   1.2.ceil           # => 2
  #   1.234.ceil(2)      # => 1.24
  #   (-1.2).ceil        # => -1
  def ceil: (?Integer) -> Numeric

  # Returns true when self is neither infinite nor NaN.
  #
  #   1.5.finite?         # => true
  #   (1.0/0).finite?     # => false
  def finite?: () -> bool

  # Returns the largest number less than or equal to self, with optional precision.
  #
  #   1.8.floor          # => 1
  #   1.234.floor(2)     # => 1.23
  #   (-1.2).floor       # => -2
  def floor: (?Integer) -> Numeric

  # Returns 1 for positive infinity, -1 for negative infinity, or nil otherwise.
  #
  #   (1.0/0).infinite?     # => 1
  #   (-1.0/0).infinite?    # => -1
  #   1.5.infinite?         # => nil
  def infinite?: () -> Integer?

  # Returns self rounded to the nearest value, with optional digits of precision. Ties round away from zero.
  #
  #   1.5.round          # => 2
  #   1.234.round(2)     # => 1.23
  #   1.235.round(2)     # => 1.24
  def round: (?Integer) -> Numeric

  # Returns self.
  #
  #   1.5.to_f    # => 1.5
  def to_f: () -> Float

  # Returns self truncated to an Integer.
  #
  #   3.7.to_i      # => 3
  #   (-3.7).to_i   # => -3
  def to_i: () -> Integer

  # Returns self truncated toward zero, with optional digits of precision.
  #
  #   1.7.truncate         # => 1
  #   1.234.truncate(2)    # => 1.23
  #   (-1.7).truncate      # => -1
  def truncate: (?Integer) -> Numeric

  # Returns a two-element array containing the floor quotient and the modulus.
  #
  #   11.5.divmod(3.25)    # => [3, 1.75]
  def divmod: (Numeric) -> Array[Numeric]

  # Returns the string representation of self. Special values are formatted as "Infinity", "-Infinity", or "NaN".
  #
  #   1.5.to_s         # => "1.5"
  #   (1.0/0).to_s     # => "Infinity"
  def to_s: () -> String

  # Returns a string representation of self for debugging.
  #
  #   1.5.inspect     # => "1.5"
  def inspect: () -> String

  # Returns true if self is a "Not a Number" value, otherwise false.
  #
  #   (0.0/0.0).nan?    # => true
  #   1.5.nan?          # => false
  def nan?: () -> bool

  # Returns the absolute value of self.
  #
  #   3.5.abs      # => 3.5
  #   (-3.5).abs   # => 3.5
  def abs: () -> Float

  # Returns a hash code for self, suitable for use as a Hash key.
  #
  #   1.5.hash == 1.5.hash    # => true
  def hash: () -> Integer

  # Returns true when the argument is also a Float with the same value.
  #
  #   1.0.eql?(1.0)    # => true
  #   1.0.eql?(1)      # => false
  def eql?: (untyped) -> bool

  # Returns the floating-point quotient of self divided by the argument.
  #
  #   5.0.quo(2)     # => 2.5
  def quo: (Numeric) -> Float

  # Returns the integer floor of self divided by the argument.
  #
  #   11.5.div(3)    # => 3
  #   11.5.div(3.5)  # => 3
  def div: (Numeric) -> Integer

end
