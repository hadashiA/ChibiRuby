# <auto-generated />
# Source: MRubyCS.StdLib.ArrayMembers

class Array[Elem] < Object
  def ==: (untyped) -> bool

  def eql?: (untyped) -> bool

  def push: (*Elem) -> self
  alias << push

  def +: (Array[Elem]) -> Array[Elem]

  def slice: (int) -> Elem | (int, int) -> Array[Elem]? | (Range[int]) -> Array[Elem]?
  alias [] slice

  def []=: (int, Elem) -> Elem | (int, int, Elem) -> Elem | (Range[int], Elem) -> Elem

  def *: (Integer) -> Array[Elem] | (String) -> String

  def concat: (*Array[Elem]) -> self

  def size: () -> Integer
  alias length size

  def empty?: () -> bool

  def first: () -> Elem | (int) -> Array[Elem]

  def last: () -> Elem | (int) -> Array[Elem]

  def reverse: () -> Array[Elem]

  def reverse!: () -> self

  def pop: () -> Elem

  def delete_at: (int) -> Elem

  def clear: () -> self

  def index: (untyped) -> Integer?

  def rindex: (untyped) -> Integer?

  def join: (?String) -> String

  def replace: (Array[Elem]) -> self
  alias initialize_copy replace

  def shift: () -> Elem | (Integer) -> Array[Elem]

  def unshift: (*Elem) -> self

  def to_s: () -> String

  def inspect: () -> String

  def deconstruct: () -> Array[Elem]

  def __ary_eq: (untyped) -> bool

  def __ary_cmp: (untyped) -> Integer?

  def __svalue: () -> Elem

  def self.[]: (*Elem) -> Array[Elem]

  # --- from lib.rb ---
  #
  # call-seq:
  #   array.each {|element| ... } -> self
  #   array.each -> Enumerator
  #
  # Calls the given block for each element of +self+
  # and pass the respective element.
  #
  # ISO 15.2.12.5.10
  def each: () ?{ (?) -> untyped } -> (untyped | self)

  #
  # call-seq:
  #   array.each_index {|index| ... } -> self
  #   array.each_index -> Enumerator
  #
  # Calls the given block for each element of +self+
  # and pass the index of the respective element.
  #
  # ISO 15.2.12.5.11
  def each_index: () ?{ (?) -> untyped } -> (untyped | self)

  #
  # call-seq:
  #   array.collect! {|element| ... } -> self
  #   array.collect! -> new_enumerator
  #
  # Calls the given block for each element of +self+
  # and pass the respective element. Each element will
  # be replaced by the resulting values.
  #
  # ISO 15.2.12.5.7
  def collect!: () ?{ (?) -> untyped } -> (untyped | self)

  #
  # call-seq:
  #   array.map! {|element| ... } -> self
  #   array.map! -> new_enumerator
  #
  # Alias for collect!
  #
  # ISO 15.2.12.5.20
  alias map! collect!

  #
  # Private method for Array creation.
  #
  # ISO 15.2.12.5.15
  def initialize: (?::Integer size, ?untyped? obj) ?{ (?) -> untyped } -> void

  #
  # call-seq:
  #   array == other   -> true or false
  #
  #  Equality---Two arrays are equal if they contain the same number
  #  of elements and if each element is equal to (according to
  #  Object.==) the corresponding element in the other array.
  #
  def ==: (untyped other) -> (false | true | untyped)

  #
  # call-seq:
  #   array.eql? other_array -> true or false
  #
  #  Returns <code>true</code> if +self+ and _other_ are the same object,
  #  or are both arrays with the same content.
  #
  def eql?: (untyped other) -> (false | true | untyped)

  #
  # call-seq:
  #   array <=> other_array -> -1, 0, or 1
  #
  #  Comparison---Returns an integer (-1, 0, or +1)
  #  if this array is less than, equal to, or greater than <i>other_ary</i>.
  #  Each object in each array is compared (using <=>). If any value isn't
  #  equal, then that inequality is the return value. If all the
  #  values found are equal, then the return is based on a
  #  comparison of the array lengths. Thus, two arrays are
  #  "equal" according to <code>Array#<=></code> if and only if they have
  #  the same length and the value of each element is equal to the
  #  value of the corresponding element in the other array.
  #
  def <=>: (untyped other) -> (0 | nil | untyped)

  #
  # call-seq:
  #   array.delete(obj) -> deleted_object
  #   array.delete(obj) {|nosuch| ... } -> deleted_object or block_return
  #
  # Delete element with index +key+
  def delete: (untyped key) ?{ (?) -> untyped } -> untyped

  #
  # call-seq:
  #   array.sort! -> self
  #   array.sort! {|a, b| ... } -> self
  #
  # Sort all elements and replace +self+ with these
  # elements.
  def sort!: () ?{ (?) -> untyped } -> self

  #
  # call-seq:
  #   array.sort -> new_array
  #   array.sort {|a, b| ... } -> new_array
  #
  # Returns a new Array whose elements are those from +self+, sorted.
  def sort: () { (?) -> untyped } -> untyped

  #
  # call-seq:
  #   array.to_a -> self
  #
  # Returns self, no need to convert.
  def to_a: () -> self

  alias entries to_a

  #
  # Array is enumerable
  # ISO 15.2.12.3
  include Enumerable
  # --- from lib.rb ---
  def sort_by: () ?{ (?) -> untyped } -> untyped

  def sort_by!: () { (?) -> untyped } -> untyped
end
