# <auto-generated />
# Source: ChibiRuby.StdLib.RegexpMembers

# Regular expression literal -- written as /pattern/flags. Matching against a String via =~ or match returns a MatchData (or nil on no match). In ChibiRuby, the pattern is translated to and executed by .NET's Regex, so some Ruby-specific syntax may differ.
class Regexp < Object
  # Constructs a new Regexp from the given pattern string and option flags. When the first argument is itself a Regexp, returns a copy.
  #
  #   r = Regexp.new("hello", Regexp::IGNORECASE)
  #   r.match?("Hello")   # => true
  def initialize: (String, ?(Integer | bool)) -> Regexp

  # Matches self against the given string starting at the optional character position. Returns a MatchData object on success, or nil on failure.
  #
  #   /(\w+)/.match("hello world")[0]   # => "hello"
  #   /xyz/.match("hello")              # => nil
  def match: (String, ?Integer) -> MatchData?

  # Returns true if the pattern matches the given string. Does not allocate a MatchData or update match-related global variables.
  #
  #   /\d+/.match?("abc 42")   # => true
  #   /xyz/.match?("abc")      # => false
  def match?: (String, ?Integer) -> bool

  # Matches the pattern against the given string and returns the character index of the first match, or nil when there is no match.
  #
  #   /world/ =~ "hello world"   # => 6
  #   /xyz/   =~ "hello"         # => nil
  def =~: (String?) -> Integer?

  # Case-equality operator. Returns true if the pattern matches the argument. Used by case/when.
  #
  #   case "hello"
  #   when /^h/ then "starts with h"
  #   end                            # => "starts with h"
  def ===: (untyped) -> bool

  # Returns the original pattern string of self, without surrounding slashes or option flags.
  #
  #   /hello/i.source   # => "hello"
  def source: () -> String

  # Returns the set of options flags used to create self as an integer bitmask (IGNORECASE=1, EXTENDED=2, MULTILINE=4).
  #
  #   /hello/i.options   # => 1
  def options: () -> Integer

  # Returns true when self was compiled with the case-insensitive option.
  #
  #   /hello/i.casefold?   # => true
  #   /hello/.casefold?    # => false
  def casefold?: () -> bool

  # Returns a string in the "(?opts-opts:pattern)" form that, when compiled again, reproduces the same pattern and options.
  #
  #   /hello/i.to_s   # => "(?i-mx:hello)"
  def to_s: () -> String

  # Returns a literal-style representation of self, like "/pattern/flags".
  #
  #   /hello/i.inspect   # => "/hello/i"
  def inspect: () -> String

  # Returns true when the argument is a Regexp with the same pattern and options as self.
  #
  #   /hello/i == /hello/i   # => true
  #   /hello/i == /hello/    # => false
  def ==: (untyped) -> bool

  # Returns true when the argument is an equal Regexp. Equivalent to == for Regexp.
  #
  #   /a/.eql?(/a/)   # => true
  def eql?: (untyped) -> bool

  # Returns a hash code computed from the pattern and options of self.
  #
  #   /a/.hash.class   # => Integer
  def hash: () -> Integer

  # Returns a hash mapping each named capture group in self to an array containing its group index.
  #
  #   /(?<year>\d{4})/.named_captures   # => {"year" => [1]}
  def named_captures: () -> Hash[String, Array[Integer]]

  # Returns the list of named capture group names defined in self.
  #
  #   /(?<y>\d+)-(?<m>\d+)/.names   # => ["y", "m"]
  def names: () -> Array[String]

  # Alias for Regexp.new. Compiles the pattern string into a Regexp.
  #
  #   Regexp.compile("\\d+").match?("abc 42")   # => true
  def self.compile: (String, ?(Integer | bool)) -> Regexp

  # Returns a copy of the given string with regular-expression metacharacters escaped, so that the result matches the original literally.
  #
  #   Regexp.escape("a.b*c")   # => "a\\.b\\*c"
  def self.escape: (String) -> String

  # Alias for Regexp.escape. Returns a string with regex metacharacters escaped.
  #
  #   Regexp.quote("1+1")   # => "1\\+1"
  def self.quote: (String) -> String

  # Returns a Regexp that matches any of the given patterns, joined with alternation. Strings are escaped automatically; Regexp arguments preserve their options.
  #
  #   r = Regexp.union("foo", "bar")
  #   r.match?("bar")   # => true
  def self.union: (*untyped) -> Regexp

  # Returns the argument if it is a Regexp, otherwise nil.
  #
  #   Regexp.try_convert(/x/)   # => /x/
  #   Regexp.try_convert("x")   # => nil
  def self.try_convert: (untyped) -> Regexp?

  # Returns the MatchData from the last successful pattern match in the current scope, or the nth capture when an index is given.
  #
  #   /(\w+)/ =~ "hello"
  #   Regexp.last_match[0]   # => "hello"
  def self.last_match: (?Integer) -> untyped

end
