# <auto-generated />
# Source: MRubyCS.StdLib.RangeMembers

# An interval between two values, written as begin..end (inclusive) or begin...end (exclusive of end). Used for iteration ((1..10).each), membership tests ((1..10).include?(x)), and array slicing. Includes Enumerable when the endpoints support succ.
class Range[Elem] < Object
  include Enumerable

  # Returns the begin value of the range.
  #
  #   (1..5).begin    # => 1
  #   (1...5).begin   # => 1
  def begin: () -> Elem

  # Returns the end value of the range (regardless of whether the end is excluded).
  #
  #   (1..5).end      # => 5
  #   (1...5).end     # => 5
  def end: () -> Elem

  # Returns the begin value of the range.
  #
  #   (1..5).first    # => 1
  def first: () -> Elem

  # Returns the end value of the range (regardless of whether the end is excluded).
  #
  #   (1..5).last     # => 5
  #   (1...5).last    # => 5
  def last: () -> Elem

  # Returns true when the end value is excluded from the range.
  #
  #   (1..5).exclude_end?    # => false
  #   (1...5).exclude_end?   # => true
  def exclude_end?: () -> bool

  # Returns true when the argument is within the range (using <=>).
  #
  #   (1..5).include?(3)     # => true
  #   (1..5).include?(5)     # => true
  #   (1...5).include?(5)    # => false
  def member?: (untyped) -> bool
  alias === member?
  alias include? member?

  # Returns true when the argument is a range whose endpoints are eql? to those of self, with the same exclusivity. Stricter than ==.
  #
  #   (1..5).eql?(1..5)      # => true
  #   (1..5).eql?(1.0..5.0)  # => false
  def eql?: (untyped) -> bool

  # Initializes a new range from begin to end. If the third argument is true, the end value is excluded.
  #
  #   Range.new(1, 5)        # => 1..5
  #   Range.new(1, 5, true)  # => 1...5
  def initialize: (Elem, Elem, ?bool) -> void

  # Replaces self with a copy of the given range. Used by dup and clone.
  #
  #   (1..5).dup    # => 1..5
  def initialize_copy: (Range[Elem]) -> self

  # Returns true when the argument is a range with equal endpoints and the same exclusivity.
  #
  #   (1..5) == (1..5)    # => true
  #   (1..5) == (1...5)   # => false
  def ==: (untyped) -> bool

  # Returns a string of the form "begin..end" or "begin...end".
  #
  #   (1..5).to_s     # => "1..5"
  #   (1...5).to_s    # => "1...5"
  def to_s: () -> String

  # Returns a string representation of the range using inspect on each endpoint.
  #
  #   (1..5).inspect      # => "1..5"
  #   ("a".."c").inspect  # => "\"a\"..\"c\""
  def inspect: () -> String

  # Internal helper used by to_a for numeric ranges.
  def __num_to_a: () -> Array[Elem]?

  # --- from lib.rb ---

  # Calls the given block for each element of +self+
  # and pass the respective element.
  #
  # ISO 15.2.14.4.4
  def each: () ?{ (?) -> untyped } -> (untyped | self)

  # redefine #hash 15.3.1.3.15
  def hash: () -> untyped

  # call-seq:
  #    rng.to_a                   -> array
  #    rng.entries                -> array
  #
  # Returns an array containing the items in the range.
  #
  #   (1..7).to_a  #=> [1, 2, 3, 4, 5, 6, 7]
  #   (1..).to_a   #=> RangeError: cannot convert endless range to an array
  def to_a: () -> untyped

  alias entries to_a
end
